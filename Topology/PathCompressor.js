'use strict';
/*
 * Compress/decompress destinalist list with opaque ids
 */
const DestinationList = require('../Transport/messages/header/DestinationList.js');

module.exports = class PathCompressor {
  constructor() {}
  compress(destList) {
    out.error('Compression of DestinationList is not supported yet!');
  }
  decompress(id) {
    out.error('Decompression of OpaqueID is not supported yet!');
  }
}
